# Production Environment Variables
# Copy this file to .env and update the values

# Database Configuration
# Replace <RENDER_DB_PASSWORD> with the password from your Render database dashboard
# Full example using your hosted database (keep this line in sync with Render):
# postgresql://bookit_user:<RENDER_DB_PASSWORD>@dpg-d3cpdkili9vc73dpovmg-a.oregon-postgres.render.com/bookit_db_sflo
DATABASE_URL=postgresql://bookit_user:<RENDER_DB_PASSWORD>@dpg-d3cpdkili9vc73dpovmg-a.oregon-postgres.render.com/bookit_db_sflo
TESTING=false

# JWT Configuration
# Generate a 32+ character secret key with:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=<GENERATED_SECRET_KEY>
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Application Configuration
ENVIRONMENT=production
DEBUG=false
API_V1_STR=/api/v1
PROJECT_NAME=BookIt API
VERSION=1.0.0

# CORS Configuration (JSON array format)
BACKEND_CORS_ORIGINS=["http://localhost:3000", "https://your-frontend-domain.com"]

# Admin User Configuration (for initial setup)
# Replace placeholders below before uploading to Render
ADMIN_EMAIL=maureenonovae593@gmail.com
ADMIN_PASSWORD=<ADMIN_PASSWORD>
ADMIN_NAME=Maureen Onovae

# Logging Configuration
LOG_LEVEL=INFO

# Security Settings
DOCS_URL=null  # Disable docs in production
REDOC_URL=null  # Disable redoc in production